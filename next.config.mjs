/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['aceternity.com','res.cloudinary.com','images.unsplash.com', 'barkinali.com'],
    },
  };
  
  export default nextConfig;
  